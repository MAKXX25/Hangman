# Optional Pusher configuration for multi-device realtime synchronization on Vercel
# If omitted, single-player PvE works 100% locally and multi-tab testing works via browser BroadcastChannel!
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=mt1
PUSHER_APP_ID=
PUSHER_SECRET=
